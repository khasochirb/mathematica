// Refinement-loop runner page. Resumes the user's active loop (or shows an
// empty state) — see components/esh/RefinementLoop. Entry is via the
// "Бүрэн эзэмших" button on missed questions (SimilarQuestionsPanel).
import RefinementLoop from "@/components/esh/RefinementLoop";

export default function RefinementLoopPage() {
  return <RefinementLoop />;
}
